const helper = {
  uppercase(str) {
    return str.toUpperCase();
  },
  lovercase(str) {
    return str.toLowerCase();
  },
};

console.log(helper.uppercase('selam'));
console.log(helper.lovercase('SELAM'));
